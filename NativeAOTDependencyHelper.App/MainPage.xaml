<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="NativeAOTDependencyHelper.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:NativeAOTDependencyHelper.App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:NativeAOTDependencyHelper.Core.Models"
    xmlns:viewModels="using:NativeAOTDependencyHelper.ViewModels"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:TaskResultConverter x:Key="TaskResultConverter" />
    </Page.Resources>

    <Grid Margin="8" ColumnSpacing="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button
            Click="OpenSolution_Click"
            Content="Open Sln..."
            IsEnabled="{x:Bind local:MainPage.IsTaskSuccessful(ViewModel.DotnetVersionCommand.ExecutionTask.Status), Mode=OneWay}" />
        <TextBlock
            Grid.Column="1"
            HorizontalAlignment="Right"
            Text="{x:Bind ViewModel.DotnetVersionCommand.ExecutionTask, Converter={StaticResource TaskResultConverter}, Mode=OneWay}" />
        <!--  List of dependencies grouped by project owner?  -->
        <ListView
            x:Name="DependencyView"
            Grid.Row="1"
            ItemsSource="{x:Bind ViewModel.Packages, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:NuGetPackageViewModel">
                    <Grid ColumnDefinitions="Auto,Auto" RowDefinitions="Auto,Auto,Auto">
                        <TextBlock
                            Grid.ColumnSpan="2"
                            Style="{ThemeResource TitleTextBlockStyle}"
                            Text="{x:Bind Info.Name}" />
                        <TextBlock Grid.Row="1">
                            <Run Text="{x:Bind Info.ParentProjectPath}" />
                            <Run FontSize="10" Text="(" />
                            <Run FontSize="10" Text="{x:Bind Info.Framework}" />
                            <Run FontSize="10" Text=")" />
                        </TextBlock>
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="1"
                            Text="{x:Bind Info.ResolvedVersion}" />
                        <!--  TODO: These are the details...  -->
                        <ItemsControl
                            Grid.Row="2"
                            Grid.ColumnSpan="2"
                            ItemsSource="{x:Bind CheckItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="models:AOTCheckItem">
                                    <Grid ColumnDefinitions="Auto,*,Auto">
                                        <TextBlock Style="{ThemeResource CaptionTextBlockStyle}" Text="{x:Bind Provider.Name}" />
                                        <TextBlock Grid.Column="1" Text="{x:Bind ReportDetails}" />
                                        <!--  TODO: Have status/icons here... (queued, in progress, fail, pass)  -->
                                        <!--  TODO: Do we want more than bool for checks? Success, Warning, Failure?  -->
                                        <TextBlock
                                            Grid.Column="2"
                                            FontWeight="Bold"
                                            Text="{x:Bind HasPassed}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <!--  Details Page  -->
    </Grid>
    <!--  TODO: We should have a status with a progress bar and the count of reports done vs. total, etc...  -->
    <!--  TODO: Can we cancel all this? (i.e. add cancel button?)  -->
</Page>
